server {
    sendfile off;

    listen 80 default_server;
    server_name nott.local;
    root /var/www/notes/project/;

    location / {
        proxy_pass http://localhost:5000;
    }

    location /static/ {
        try_files $uri $uri/ =404;
    }
}